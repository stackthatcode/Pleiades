Your Order has been Received!  Your Order Number is: @Model.Order.ExternalId.  

It will ship to the following address:

@Model.Order.Name
@Model.Order.Address1 @Model.Order.Address2
@Model.Order.City, @Model.Order.State @Model.Order.ZipCode

Your Order contains the following items:

@foreach (var orderline in Model.OrderLineGroups) 
{
	@(orderline.Name + " (" + orderline.SkuCode + ") - quantity: " + orderline.Quantity + "\n") 
}
