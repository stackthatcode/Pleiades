@model Commerce.Application.Orders.Entities.OrderRefund
Items from Order #@Model.Order.ExternalId have been Refunded.

@foreach (var orderline in Model.OrderLineGroups) 
{
	@(orderline.Name + " (" + orderline.SkuCode + ") - quantity: " + orderline.Quantity + "\n") 
}

Total Amount: @Model.Transaction.Amount.ToString("$0.00")
